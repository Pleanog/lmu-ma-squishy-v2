# Ignore Python virtual environments
venv/
ENV/

# Ignore PocketBase data and temporary files
**/pb_data/
**/*.exe
**/*.db

**/*.env

# Local TLS certificates (dev only, never commit private keys)
backend/server/certs/
frontend/certs/