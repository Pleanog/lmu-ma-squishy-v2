

This project consists of a FastAPI WebSocket Backend, a Vue.js Frontend (GeminiZwo.vue), and a Raspberry Pi Hardware client. The goal is to improve usability, tracking, and routing.

🛠️ Phase 1: Database & User Context (Backend & Frontend)

    Dynamic Usernames: Remove the old login logic in the Vue frontend. Replace it with a simple text input to set a "Username" before starting a session.

    System Prompt Injection: When the backend initializes the GeminiSessionManager, it must receive the Username from the frontend and dynamically inject it into the Gemini system_prompt (e.g., "Du sprichst gerade mit [Username]").

    PocketBase Logging Setup: Implement a logging service in FastAPI to send data to PocketBase. We need to save:

        Full user inputs (Text or transcribed Voice).

        Full AI responses (Text or transcribed Voice).

        Hardware Tool Calls & Sensor Events.

        Rule: Only save completed sentences/turns, no interim fragments. Timestamps are added automatically by pocketbase.


        Dabei habe ich bereits folgende Collections in PocketBase, diese und der inhalt können noch angepasst werden sollte der code hier das benötigen, gerade hardware event oder sonstige dinge die schon eher fix sind, das schema ist noh sehr flexibel:
        
        - The backend creates a new entry in the `conversations` collection when a new participant starts a new session.
        - **ALL** interactions are logged into the `messages` collection chronologically. Map the data as follows:
            - `conversation`: Relation ID to the active conversation.
            - `sender`: Use `"user"` (for transcripts/gestures), `"llm"` (for AI speech), or `"system"` (for tool executions/errors).
            - `content`: The spoken text, gesture name, or tool name.
            - `processing_time`: Calculate and store latency in ms (especially for `llm` responses).
            - `metadata`: A JSON object for extra context. 
            - For transcripts: `{"modality": "text_ui" | "voice_hardware", "was_interrupted": true/false}`
            - For hardware events/gestures: `{"event_type": "gesture", "args": {...}}`
            - For tools: `{"event_type": "tool_call", "args": {...}}`

            collections look like:
            conversations
                id (generated by PB)
                title
                is_active
                Nonempty
                participant_id
                created (generated by PB)
                updated (generated by PB)
                Unique constraints and indexes (0)
            
            messages
                id (generated by PB)
                conversation relation to conversations (Single)
                content
                sender (can be "user", "llm", "system", "admin") (Single)
                audio (Multiple) (currently not used)
                image (Multible) (currently not used)
                status (can be pending • sent • delivered • failed • processing) (Single)
                processing_time
                metadata
                created (generated by PB)
                updated (generated by PB)
                Unique constraints and indexes (0)


🛠️ Phase 2: Multi-Client Routing (Backend & Frontend)

Goal: Both Frontend and Hardware can be active simultaneously without clashing.

    Step 1: Modality Routing:

        If the user types text in the Vue UI -> Gemini responds with Text -> Display in Vue UI.

        If the user speaks into the Hardware Pi -> Gemini responds with Audio -> Play on Pi.

    Shared State: Regardless of who initiated the turn, the resulting transcript (both User and AI) must ALWAYS be broadcasted to the Vue UI for display. This allready workd perfelcty.

    Routing Control UI: Add a second audio controll modal (simmilar to the settings modal) in the Vue frontend to toggle routing settings (e.g., "Hardware Mic: ON/OFF", "Hardware Speaker: ON/OFF", "UI Text Mode: ON/OFF", UI Speaker On / Off, Mic ON/OFF). We do not need to disable the hardware sound or mic we can just simply not send or recive data to these two then, i think this is a simple and effective way of handeling this, the hardware streams and listens to input but the server simply refuses to send it. The Ai is not interrupted!

    Step 2: Same gestures for the hardware and frontend
        I also currently have the frontend with the right gestures.
        now i need to map them correclty for the hardware.

        Here are the gestures defined for the different sensors i implemented in the hardware! Please adjust them accordingly:

          {
            code: 'R1_Activate',
            backendEvent: 'press_head',
            is when the touch sensor registers that it was touched or when the squish sensor senses that it was deformed
          },
          {
            code: 'R2_Stop',
            backendEvent: 'hush',
            is when the fron / face of the touch sensing area is touched
          },
          {
            code: 'R3_Concise',
            name: 'Concise',
            backendEvent: 'drop_on_table',
            is when the hardware is picked up and placed back on the table, so the gyro sensor needs to detect this up and down movement
            },
          {
            code: 'R4_Elaborate',
            name: 'Elaborate',
            backendEvent: 'tap_head',
            when the user taps twice on the touch sensor on the head
          },
          {
            code: 'R5_Save',
            name: 'Save',
            backendEvent: 'target_focus',
            when ge gyro sensor detects that the hardware is beeing picked up and moved horizontally in one direction (here a crude implementations is ok for now)
          },
          {
            code: 'R6_NewSession',
            name: 'New Session',
            backendEvent: 'horizontal_turn',
            gyro sensor detects a turn to the left or right horizontally
          },
          {
            code: 'R7_Options',
            name: 'Options',
            backendEvent: 'shake',
            gyrosensor detects shaking vertically or horizontally
          },
          {
            code: 'R8_Optimize',
            name: 'Optimize',
            backendEvent: 'squeeze',
            we do not detect squising but just when the touch sensor detects that both lef and right side are touched at the same time
          },




🛠️ Phase 3: Server Admin & Hardware Control (Frontend & Backend)

    Step 1.
      Dynamic API Key: Add an input field in the Vue UI / Settings Modal to set/update the Google Gemini API Key dynamically, preventing the need to restart the FastAPI server manually.
      This needs to make sure that when the api key was changed the old session with the old key is removed totally.

      Session Control: Add a "Force Restart Session" button in the frontend that sends a signal to the backend to gracefully close and reopen the Gemini Live connection. if this is not allready in place. this should be in settings modal as well, as a way to restart if a problem occurs with gemini and it does not answer anymore. I allready have the disconnect button on the ui, this can be merged if they would share the same logic, not sure. we can also keep both if they serve a differen tpurpose. all settings hsould have a little text helping with explaining what the setting does, especially the technical ones. The start mic button can move to the chat bar left side of the text input field and make rename it to: voice mode activate / deactivate? or some ui friendly term / or icon with a tooltip

      Hardware Restart: Add a "Restart Hardware Client" button in the frontend to the settings modal. The backend should broadcast a specific WebSocket command (e.g., {"type": "system_command", "action": "restart"}) to the Hardware Client. (Note: The hardware python script will handle the actual os.execv restart logic).
      i allready have a service that auto starts the hardware on power that we maybe can call like this:
      import os

      # in deiner handle_backend_message in main.py:
      elif message_type == "system_command":
          if data.get("command") == "restart":
              logger.warning("🚨 Restart-Befehl vom Frontend erhalten! Starte Service neu...")
              # Beende den WebSocket sauber
              await websocket_client.disconnect()
              # Weise das Betriebssystem an, den Service neu zu starten
              os.system("sudo systemctl restart squishy")


      In the frontend i want a new page that can be used to monitor the state of everything if possible without a lot of changes (this pages should be reachable via the setting modal as a small text link button):
      Admin Dashboard: A simple view in the frontend showing the connection status of all WebSocket clients (Frontend, Hardware) and a rough estimate of session duration/activity.

    Step 2. 
      One unified way to start server, pocketbase database, frontend and hardware at once, so it is easier to manage everything. the hardware is physically on a different device so here it might not make sense to do so. So maybe here we dont include it into our master starter scribt or however is the best solution.